<Window x:Class="ItTalksTTS.App.Views.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ItTalksTTS — Kokoro setup"
        Height="420" Width="560"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E" Foreground="#D4D4D4">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock Text="Download models, create a Python venv, install kokoro-onnx, and deploy the worker. Requires Python 3.10+ on PATH." TextWrapping="Wrap" Margin="0,0,0,12"/>
    <TextBlock x:Name="Status" Grid.Row="1" FontWeight="SemiBold" Margin="0,0,0,8"/>
    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,8">
      <TextBox x:Name="Log" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
               Background="#252526" Foreground="#CCCCCC" BorderBrush="#3C3C3C"/>
    </ScrollViewer>
    <ProgressBar x:Name="Progress" Grid.Row="3" Height="16" Minimum="0" Maximum="100" Margin="0,0,0,12"/>
    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="RunBtn" Content="Run setup" Width="120" Margin="0,0,8,0" Click="Run_Click"/>
      <Button x:Name="CancelBtn" Content="Close" Width="80" Click="Cancel_Click"/>
    </StackPanel>
  </Grid>
</Window>
